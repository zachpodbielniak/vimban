#!/bin/bash

# vimban - Markdown-native ticket/kanban management system
# Copyright (C) 2025  Zach Podbielniak
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.

# vimban_recipe - Quick recipe creation
#
# Usage:
#   vimban_recipe "Recipe title"
#   vimban_recipe --personal "Family recipe"
#
# Creates a recipe entry in the food_and_health/recipes area.
#
# Defaults:
#   reporter: zach_podbielniak

set -euo pipefail

show_help () {
    cat <<EOF
vimban_recipe - Quick recipe creation

Usage:
    vimban_recipe [options] "Recipe title"
    vimban_recipe "Recipe title" [vimban options]

Creates a recipe entry in the food_and_health/recipes area.

Defaults:
    reporter: zach_podbielniak

Options:
    -h, --help          Show this help message
    --work              Create in work scope
    --personal          Create in personal scope
    -r, --reporter REF  Override reporter (default: zach_podbielniak)
    --no-edit           Don't open editor after creation (print file path)

Any additional options are passed through to vimban create recipe.

Examples:
    vimban_recipe "Butter-basted ribeye"
    vimban_recipe "Bacon and eggs breakfast"
    vimban_recipe "Beef tallow fried burger patties"
    vimban_recipe --personal "Mom's meatloaf"
EOF
}

# Default values
scope_arg=""
reporter="${VIMBAN_REPORTER:-}"
no_edit=""

# Passthrough args (title, other options)
passthrough_args=()

# Parse arguments
while [[ $# -gt 0 ]]
do
    case "${1}" in
        -h|--help)
            show_help
            exit 0
            ;;
        --work)
            scope_arg="--work"
            shift
            ;;
        --personal)
            scope_arg="--personal"
            shift
            ;;
        -r|--reporter)
            reporter="${2}"
            shift 2
            ;;
        --no-edit)
            no_edit="--no-edit"
            shift
            ;;
        *)
            passthrough_args+=("${1}")
            shift
            ;;
    esac
done

# Check if we have at least a title in passthrough args
if [[ ${#passthrough_args[@]} -lt 1 ]]
then
    echo "Error: Recipe title required" >&2
    echo "Usage: vimban_recipe [options] \"Recipe title\"" >&2
    exit 1
fi

# Build and execute the command
# shellcheck disable=SC2086
exec vimban ${scope_arg} create recipe -r "${reporter}" ${no_edit} "${passthrough_args[@]}"
