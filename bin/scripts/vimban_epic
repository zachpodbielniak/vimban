#!/bin/bash

# vimban - Markdown-native ticket/kanban management system
# Copyright (C) 2025  Zach Podbielniak
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.

# vimban_epic - Quick epic creation with sensible defaults
#
# Usage:
#   vimban_epic "Epic title"
#   vimban_epic "Epic title" -p high
#   vimban_epic "Epic title" --due +30d
#   vimban_epic --work "Work epic"
#
# Defaults:
#   assignee: zach_podbielniak
#   reporter: zach_podbielniak
#   due_date: +7d (1 week from now)
#   priority: medium

set -euo pipefail

show_help () {
    cat <<EOF
vimban_epic - Quick epic creation with sensible defaults

Usage:
    vimban_epic [options] "Epic title"
    vimban_epic "Epic title" [vimban options]

Defaults:
    assignee: zach_podbielniak
    reporter: zach_podbielniak
    due_date: +7d (1 week from now)
    priority: medium

Options:
    -h, --help          Show this help message
    -p, --priority PRI  Override priority (critical|high|medium|low)
    --due DATE          Override due date (+Nd, YYYY-MM-DD, etc.)
    -a, --assignee REF  Override assignee
    -r, --reporter REF  Override reporter
    --work              Create in work scope
    --personal          Create in personal scope
    --no-edit           Don't open editor after creation (print file path)

Any additional options are passed through to vimban create epic.

Examples:
    vimban_epic "Refactor authentication system"
    vimban_epic "Q1 Product Launch" -p high --due +90d
    vimban_epic --work "Platform Migration"
    vimban_epic --personal "Home Renovation" -p low
EOF
}

# Default values
assignee="${VIMBAN_ASSIGNEE:-}"
reporter="${VIMBAN_REPORTER:-}"
due_date="+7d"
priority="medium"
scope_arg=""
no_edit=""

# Passthrough args (title, other options)
passthrough_args=()

# Parse arguments
while [[ $# -gt 0 ]]
do
    case "${1}" in
        -h|--help)
            show_help
            exit 0
            ;;
        -p|--priority)
            priority="${2}"
            shift 2
            ;;
        --due)
            due_date="${2}"
            shift 2
            ;;
        -a|--assignee)
            assignee="${2}"
            shift 2
            ;;
        -r|--reporter)
            reporter="${2}"
            shift 2
            ;;
        --work)
            scope_arg="--work"
            shift
            ;;
        --personal)
            scope_arg="--personal"
            shift
            ;;
        --no-edit)
            no_edit="--no-edit"
            shift
            ;;
        *)
            passthrough_args+=("${1}")
            shift
            ;;
    esac
done

# Check if we have at least a title in passthrough args
if [[ ${#passthrough_args[@]} -lt 1 ]]
then
    echo "Error: Epic title required" >&2
    echo "Usage: vimban_epic [options] \"Epic title\"" >&2
    exit 1
fi

# Build and execute the command
# shellcheck disable=SC2086
exec vimban ${scope_arg} create epic \
    -a "${assignee}" \
    -r "${reporter}" \
    --due "${due_date}" \
    -p "${priority}" \
    ${no_edit} \
    "${passthrough_args[@]}"
